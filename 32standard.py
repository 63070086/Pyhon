"""Code very hard"""
def code():
    """input x num """
    import math
    numx = float(input())
    numx = numx
    numx = (1/((2*math.pi)**0.5))*((math.e)**(-0.5*numx**2))
    print("%0.05f"%(numx))
code()
